import type { NextPage } from 'next';

import Layout from '../layout';
import Hero from '../components/hero';

const Home: NextPage = () => (
  <Layout>
    <Hero />
  </Layout>
);

export default Home;
